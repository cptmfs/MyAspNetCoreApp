@{
    ViewData["Title"] = "Index";
}

@model List<ProductViewModel>



<h1>Ürünler</h1>
@if (TempData["status"] != null)
{
    <div class="alert alert-success" role="alert">
       @TempData["status"]
    </div>

}

<a class="btn btn-primary" asp-controller="products" asp-action="add">Ürün Ekle</a>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>İsim</th>
            <th>Fiyat</th>
            <th>Stok</th>
            <th>Color</th>
            <th>Yayınlansın mı ?</th>
            <th>İndirim Oranı</th>
            <th>Yayınlanma Tarihi</th>
            <th>Ürün Açıklaması</th>
            <th></th>
            <th>İşlem</th>


        </tr>

    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Price</td>
                <td>@item.Stock</td>
                <td>@item.Color</td>
                <td>
                    @(item.IsPublish ? "Evet" : "Hayır")

                </td>
                <td>@item.Discount</td>
                <td>@item.PublishDate</td>
                <td>
                    @if (item.Description.Length>20)
                    {
                        @item.Description.Substring(0,20)<text>...</text>
                    }
                    else
                    {
                        @item.Description
                    }
                </td>


                <td>
                    <a class="btn btn-danger" asp-asp-controller="products" asp-action="remove" asp-route-id="@item.Id">Sil</a>
                </td>
                <td>
                    <a class="btn btn-success" asp-asp-controller="products" asp-action="update" asp-route-id="@item.Id">Güncelle</a>
                </td>

            </tr>
        }
    </tbody>

</table>

